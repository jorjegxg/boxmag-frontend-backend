---
description: Document Cypress e2e tests and always report pass/fail results
globs: boxmag4/cypress/**/*
alwaysApply: false
---

# Cypress test documentation

Whenever you create or update a Cypress e2e spec under `boxmag4/cypress/e2e/`, also create or update a matching markdown file in `boxmag4/cypress/documentation/`.

Do this by default — the user should not need to ask.

## Doc format (match existing docs)

Follow the structure of `currency-eur-ron.md` / `language-i18n.md`:

1. Title + Spec path + UI/State notes
2. **Cum funcționează** — behavior table
3. **Scenarii Cypress** — numbered steps with **CHECK** assertions
4. **Selectori utili** — Cypress selectors
5. **Rulare** — `npx cypress run --spec …`

Name the doc after the feature (e.g. `currency-eur-ron.md` for `currency-eur-ron.cy.ts`).

## Report test results (required)

After every Cypress run (or when reading a Cypress terminal/output), tell the user the outcome clearly:

- If **any** test failed: say **FAILED** first, then counts (e.g. `0 passing, 1 failing`), the failing test title, and the assertion/error message.
- If all passed: say **PASSED** with counts (e.g. `11/11 passing`).
- Never imply success when the run failed, timed out, or could not start (e.g. `baseUrl` unreachable).
- Do not skip or soften a failure while fixing — report the fail, then fix and re-run.
